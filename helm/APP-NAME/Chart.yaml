apiVersion: v1
appVersion: 0.0.1
name: {APP-NAME}
description: A Helm chart for {APP-NAME}
engine: gotpl
home: https://github.com/giantswarm/{APP-NAME}-app

# If you have an icon/logo, you can add it to the app repo
# or to https://github.com/giantswarm/web-assets
# and set the URL here and uncomment.
#icon: https://example.com/icon.svg

sources:
- https://github.com/some-org/some-repo
version: 0.1.0 # [[ .Version ]] if you're using architect instead of app-build-suite
annotations:
  branch: my-branch # Used through .Chart.Annotations.branch (This is not replaced automatically)
  commit: f00bar # Used through .Chart.Annotations.commit (This is not replaced automatically)
